SERVER_PORT=8080

# Database (uncomment + fill once spec-delta produces a schema)
# DATABASE_URL=jdbc:postgresql://localhost:5432/app_db
# DATABASE_USER=app_user
# DATABASE_PASSWORD=app_password

# Redis (uncomment if redis.enabled in docs/project-info.yaml)
# REDIS_HOST=localhost
# REDIS_PORT=6379
